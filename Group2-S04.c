#include "func.c"

int main()
{
    bool bGood = false, bGo = true, bStart = true, bFound = false, bOver;
    int nVal = 0;

    coords pos;

    grid R, B, S, T;

    emptySet(R); emptySet(B);
    emptySet(S); emptySet(T);

    return 0;
}